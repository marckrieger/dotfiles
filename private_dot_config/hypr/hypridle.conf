general {
    lock_cmd = omarchy-lock-screen                         # lock screen and 1password
    before_sleep_cmd = loginctl lock-session               # lock before suspend.
    after_sleep_cmd = sleep 1 && hyprctl dispatch dpms on  # delay for PAM readiness, then turn on display.
    inhibit_sleep = 3                                      # wait until screen is locked
}

# All timeout listeners removed — no screensaver, no auto-lock, no auto-sleep.
# Screen will still lock on manual suspend/sleep via before_sleep_cmd above.
